# Docker Compose Environment Variables Example
# Copy this file to .env.docker and customize the values as needed
# This is the single source of truth for Docker port configuration

# Database Port Configuration
EXTERNAL_DB_PORT=5433  # Port for external database connections (from host)
INTERNAL_DB_PORT=5432  # Internal Docker port (don't change unless you know what you're doing)

# Web Server Port Configuration
WEB_PORT=80            # External port for web access

# PHP-FPM Port Configuration
PHP_FPM_PORT=9000      # External port for PHP-FPM (usually not needed externally)

# Instructions:
# 1. Copy this file: cp .env.docker.example .env.docker
# 2. Edit .env.docker with your preferred port numbers
# 3. Restart Docker containers: docker-compose down && docker-compose up -d