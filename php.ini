; Max execution time (0 = unlimited)
max_execution_time = 0

; Memory limit (allow large operations)
memory_limit = 2G

; Upload / POST limits
post_max_size = 200M
upload_max_filesize = 200M

; Keep scripts alive during large backup
request_terminate_timeout = 0
